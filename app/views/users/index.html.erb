


<div id="user_nav">
  <% if current_user %>
    Logged in as <%= current_user.email %>
<%= link_to("Log out", session_url(:id), method: :delete) %>
  <% else %>
    <%= link_to "Sign Up", new_user_url %> or 
   <%= link_to "Log in", new_session_url %>
  <% end %>
</div>

